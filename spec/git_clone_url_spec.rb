require 'spec_helper'

describe GitCloneUrl do
  it 'has a version number' do
    expect(GitCloneUrl::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
